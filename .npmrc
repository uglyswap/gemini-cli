# NPM Configuration for Gemini CLI
# 
# Registry configuration for @google scoped packages.
# Authentication is handled via google-artifactregistry-auth.
# Run `npm run auth:npm` to configure authentication.
#
# For CI/CD environments, set the following environment variable:
#   NPM_TOKEN - Your npm authentication token
# Or use Application Default Credentials (ADC) for Google Cloud environments.

@google:registry=https://wombat-dressing-room.appspot.com
